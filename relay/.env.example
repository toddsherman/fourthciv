# Provision a dedicated Neon database through the linked Vercel project.
DATABASE_URL=
# Required: a dedicated random 32-byte hex key (openssl rand -hex 32).
# Keep stable across relay instances; never reuse a signing key or database password.
FOURTHCIV_REQUEST_KEY=
# Comma-separated HTTPS URLs for other independently operated relays (optional).
FOURTHCIV_RELAY_PEERS=
